---
import Layout from '../layouts/Layout.astro';


type Language = 'en' | 'el';
interface data {
	title: string;
	description: string,
	lang: Language
}

const data = {
	title: 'Χάρτης Μετρό Θεσσαλονίκης',
	description: "Εξερευνήστε τον πλήρη χάρτη του Μετρό Θεσσαλονίκης. Βρείτε όλες τις διαδρομές, σταθμούς και συνδέσεις για να σχεδιάσετε το ταξίδι σας αποτελεσματικά.",
	lang: "el" as Language,
}

---

<Layout title={data.title}  description={data.description}>
	<main>
        <h1 class="text-center">{data.title}</h1>
		<section class="text-center">
			<img src="/icons/map.svg" class="img-fluid" alt="Metro of Thessaloniki" />
		</section>
	</main>
</Layout>
<style>

</style>
